# SolidJS example with Rescript

To install dependencies:

```bash
bun install
```

To run developer watch mode (somewhat buggy):

```bash
bun run dev
```

js bundle and corresponding html are in `dist` library

Run 
```bash
bun run build
```

to generate minified version of the bundle
